# Markdown Indexing

The indexer scans Markdown files, parses headings, extracts tags, and builds an inverted index.

Search Ranking decides which document should appear first for a query.

This note links to [[search-ranking]] and also contains a deliberately broken link to [old parser notes](old-parser.md).

#search #markdown #engine
