# Rust Ownership

Ownership is the rule system Rust uses to manage memory without a garbage collector.

The topic is tightly related to [[borrow-checker]] because references must respect ownership and borrowing rules.

When a function can fail, ownership also interacts with Error Handling because values may move through `Result`.

#rust #memory #systems
