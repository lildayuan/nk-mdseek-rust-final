# Borrow Checker

The borrow checker verifies that references never outlive the data they point to.

It protects Rust programs from data races and dangling references.

Related ideas include ownership, lifetimes, and safe concurrency.

#rust #memory #compiler
