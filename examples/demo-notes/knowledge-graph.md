# Knowledge Graph

A knowledge graph shows how notes connect through internal links.

The graph becomes more useful when the system can suggest missing links instead of only displaying existing ones.

Markdown Indexing and Search Ranking both benefit from graph context.

#graph #markdown #search
