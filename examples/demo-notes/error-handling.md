# Error Handling

Rust encourages recoverable errors through `Result<T, E>` and unrecoverable failures through `panic!`.

Good error design makes command line tools easier to debug and safer to use.

This note should probably be linked from Rust Ownership, but the demo intentionally leaves that link missing.

#rust #reliability #cli
